spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
  profiles:
    include: oauth, secret
#server:
#  port: 8083
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace