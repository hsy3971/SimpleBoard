spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/simple
    username: sa
    password:
    driver-class-name: org.h2.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
  profiles:
    include: oauth
#server:
#  port: 8083
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace